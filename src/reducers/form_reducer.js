// import { CREATE_CAR } from "../actions/index";

export default function(state = [], action) {
  switch (action.type) {
    // case CREATE_CAR:
    //   return state;
    default:
      return state;
  }
}
